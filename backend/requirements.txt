fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1
httpx==0.27.2
xhtml2pdf==0.2.16
sqlalchemy==2.0.35
openai>=1.59,<2
langchain-core>=0.3,<0.4
langchain-openai>=0.2,<0.3
# NOTE: streamlit/pandas intentionally NOT here — the Streamlit UI is a
# separate deployable (streamlit_app.py + root requirements.txt) and
# streamlit's starlette>=0.46 pin conflicts with fastapi's starlette<0.39.
